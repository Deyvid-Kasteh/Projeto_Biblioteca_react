import styles from "./Footer.module.css";

function Footer() {
  return (
    <div className={`${styles.Footer_container}`}>
      <h1>Biblioteca Footer</h1>
    </div>
  );
}

export default Footer;
